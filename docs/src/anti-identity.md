# (Anti-) Identity

```@meta
CurrentModule = AntiIdClosedGroup
DocTestSetup = quote
    using AntiIdClosedGroup
end
```
## The identity
```@docs
id(k::Int64)
```

## The Anti-Identity
```@docs
anti_id(k::Int64)
```